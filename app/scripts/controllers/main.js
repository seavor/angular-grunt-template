(function() {
    'use strict';

    angular.module('app').controller('MainCtrl', ['$rootScope', '$state', 'config',
    function($rootScope, $state, config) {
        console.log('Initialize Main Controller');
    }]);

})();
