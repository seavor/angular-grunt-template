(function() {
    'use strict';

    angular.module('app').controller('InitCtrl', ['$rootScope', '$state', 'config',
    function($rootScope, $state, config) {
        console.log('Initialize Init Controller');
    }]);

})();
